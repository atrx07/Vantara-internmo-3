# Local runtime configuration. Copy to .env and replace placeholders locally.
DATABASE_URL=postgresql+psycopg://vantara:change_me@localhost:5432/vantara
MODEL_ARTIFACT_DIR=models_artifacts
LOG_LEVEL=INFO
APP_ENV=development
